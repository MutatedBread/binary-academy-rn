import React, { Component } from 'react';

import {
    List,
    Content,
    Container,
    ListItem,
    Left,
    Right,
    Body,
    Button
} from 'native-base';

export default class VideoList extends Components {
    constructor(props) {
        super(props);
    };

    render() {
        return (
            <List 
                dataArray={}
                renderRow={() => {

                }}>
            </List>
        );
    }''
}